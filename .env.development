# Shared development environment variables (committed to repository)
# Loaded by both Next.js (npm run dev) and Wrangler (npm run preview)
#
# For local secrets or overrides, create .env.development.local (gitignored)
# DO NOT put secrets in this file - use .env.development.local instead

NEXTJS_ENV=development
