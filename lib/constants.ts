/**
 * Canonical site URL used for metadata, OG images, sitemaps, etc.
 *
 * When environment-based overrides are needed (e.g. preview deploys),
 * replace this with `process.env.NEXT_PUBLIC_SITE_URL ?? SITE_URL`.
 */
export const SITE_URL = 'https://viscalyx.se'
