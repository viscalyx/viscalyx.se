interface:
  display_name: "Playwright Generate Test"
  short_description: "Generate Playwright tests from described scenarios"
  default_prompt: "Use $playwright-generate-test to generate Playwright test code for a described scenario, including selectors and assertions."
